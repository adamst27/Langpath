import { SignupForm } from "@/components/signup-form";
import React from "react";

const page = () => {
  return (
    <div className="flex justify-center items-center h-[100vh] bg-stone-50">
      <SignupForm />
    </div>
  );
};

export default page;
